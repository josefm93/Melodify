import { Button } from 'antd';

const PrimaryButton = () => {


const { size } = this.state;
    return (
        <Button>
            Download
        </Button>
    );
}

export default PrimaryButton
